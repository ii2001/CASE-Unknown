# Pencil design status

`case-unknown.pen` was not fabricated: no pen.dev/Pencil integration is available in this environment and the requirement explicitly forbids handwritten raw `.pen` JSON. The complete frame, component and token specification is in [`../docs/ui-ux-spec.md`](../docs/ui-ux-spec.md), and the implemented Vue/CSS UI is the executable design reference.
